using UnityEngine;

public class GlobalVariables : MonoBehaviour
{
    public enum Languages { english, russian, turkish, german, spanish, french };

    public enum GameModes { classical, infinite, random };
}
